# styles/
> L2 | Parent: src/CLAUDE.md

Foundation layer — CSS custom properties, keyframes, and utility classes that every component depends on.

## Members

globals.css: Complete theme system — Tailwind v4 @theme tokens, CSS vars, @font-face (Zpix), glass-panel, tech-border, scanline, arc-reactor, terminal-output, diary-content typography, all @keyframes, mobile responsive overrides

[PROTOCOL]: Update this file on any member change, then check parent CLAUDE.md
